% CS171 Problem Set 4
% Name: Justin Mac
% Date: 5/25/17
% SID: 861086907
function candidateSets = apriori_gen(L)
%Subroutine for Apriori function, generates candidate sets for each level
    candidateSets = {};
    for j = 1:size(L,2)
        for k = j+1:size(L,2)
            %if L[j] and L[k] agree on all but their last elements
            if L[j] = L[k]
                %candidateSets = union(L[j], L[k])
            %else
                %break
        end
    end
end

